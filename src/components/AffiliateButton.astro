---
interface Props {
  url: string;
  text?: string;
}

const { url, text = 'Check Price' } = Astro.props;
---
<a href={url} class="affiliate-btn" rel="sponsored nofollow" target="_blank">{text}</a>
